/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ["@data-platform/ui", "@data-platform/types", "@data-platform/designer-core", "@data-platform/sdk"]
};

export default nextConfig;
