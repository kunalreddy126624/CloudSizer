# Kubernetes Placeholder

Add deployment manifests here for:

- `api`
- `web`
- `postgres`
- `redis`
- `minio`
- ingress and secret resources

The current repository keeps the application structured so these manifests can be added without changing app code.
