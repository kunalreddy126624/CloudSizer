[project]
name = "cloudsizer"
version = "0.1.0"
description = "CloudSizer backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "pydantic>=2.10.0",
    "uvicorn>=0.34.0",
]
